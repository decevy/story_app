# Cursor — agent context

Use the project-wide guide at **[../AGENTS.md](../AGENTS.md)** (repository root).

Optional **file-scoped rules** live in **`.cursor/rules/`** as `.mdc` files with frontmatter (`description`, `globs`, `alwaysApply`).

When editing application code, see also **[../src/AGENTS.md](../src/AGENTS.md)**.
